<p>Hi <%= @user.name %>,</p>

<p>
  New sign-in to your account from <%= @session.device_description %>,
  <%= @item.created_at.strftime("%B %-d, %Y at %-I:%M %p %Z") %>.
  Wasn't you? <%= link_to "Review your sessions", user_sessions_url %>.
</p>
